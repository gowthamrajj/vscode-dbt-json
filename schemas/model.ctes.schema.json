{
  "$id": "model.ctes.schema.json",
  "title": "Schema Model CTEs",
  "description": "Array of CTE definitions within a model. CTEs are processed in order; later CTEs can reference earlier ones.",
  "type": "array",
  "minItems": 1,
  "items": { "$ref": "model.cte.schema.json" }
}
