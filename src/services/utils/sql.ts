/**
 * SQL utilities for extension.
 * Re-exports from shared since web also needs these functions.
 */

export {
  SQL_COLORS,
  SQL_HIGHLIGHTERS,
  SQL_KEYWORDS,
  sqlCleanLine,
  sqlFormat,
  sqlToHtml,
} from '@shared/sql/utils';
