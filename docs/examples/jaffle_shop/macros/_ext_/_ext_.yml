version: 2

macros:
  - name: _ext_event_date_filter
    description: Generates a where filter statement for filtering the event date range
    docs:
      show: false
  - name: _ext_event_datetime_filter
    description: Generates a where filter statement for filtering on event datetime ranges
    docs:
      show: false
  - name: _ext_partition_date_filter
    description: Generates a where filter statement for filtering the partition date range
    docs:
      show: false
