/**
 * Cross-cutting shared types.
 *
 * These are truly shared types used by multiple domains.
 * For domain-specific types, see their respective folders:
 * - API types: @shared/api/types
 * - DBT types: @shared/dbt/types
 * - Framework types: @shared/framework/types
 * etc.
 */

export * from './common';
export * from './config';
