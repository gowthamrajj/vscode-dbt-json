// Metadata can be stored directly on the class constructor.
// We use a unique symbol to avoid collisions with other decorators.
export const AGENT_PROMPT_METADATA_KEY = Symbol('agentPromptMetadata');
