{
  "$id": "model.having.schema.json",
  "title": "Schema Model Having",
  "description": "SQL HAVING",
  "anyOf": [
    { "type": "string" },
    {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "and": {
          "type": "array",
          "description": "Conditions to be combined by AND",
          "items": {
            "type": "object",
            "additionalProperties": false,
            "properties": {
              "expr": {
                "description": "SQL for the condition",
                "type": "string"
              },
              "group": {
                "$ref": "model.having.schema.json"
              },
              "subquery": { "$ref": "model.subquery.schema.json" }
            }
          }
        },
        "or": {
          "type": "array",
          "description": "Conditions to be combined by OR",
          "items": {
            "type": "object",
            "additionalProperties": false,
            "properties": {
              "expr": {
                "description": "SQL for the condition",
                "type": "string"
              },
              "group": {
                "$ref": "model.having.schema.json"
              },
              "subquery": { "$ref": "model.subquery.schema.json" }
            }
          }
        }
      },
      "required": []
    }
  ]
}
